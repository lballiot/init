#Récupérez le path complet du fichier dans lequel est écrit l’adresse IP du serveur
#DNS que vous utilisez

/etc/resolv.conf

#symbolic link -> ../var/run/resolv.conf
#output ls-l /etc/resolv.conf
#lrwxr-xr-x  1 root  wheel  22 Nov 20  2016 /etc/resolv.conf -> ../var/run/resolv.conf
